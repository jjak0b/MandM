import {template} from "./UserWidgetPhotoTemplate.js";

export const component = {
    template:template,
    props:{
        // valueType:Object
        //Side:String
        //type:File.name
        //value:Object

    }
}